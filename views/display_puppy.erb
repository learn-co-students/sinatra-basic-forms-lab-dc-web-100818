<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
  </head>
  <body>
    Name: <%= "#{@name}" %>
    Breed: <%= "#{@breed}" %>
    Age: <%= "#{@age}" %>
  </body>
</html>
